name: CodeQL
on:
  push:
    branches: [main, MAROON-staging]
  pull_request:
  schedule:
    - cron: '30 4 * * 1'
  workflow_dispatch:

jobs:
  analyze:
    uses: github/codeql-action/.github/workflows/codeql.yml@v2
    with:
      languages: javascript
      queries: security-extended
